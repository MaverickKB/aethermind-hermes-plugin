# Contributing

Keep the OSS primitive narrow: package behavior, tests, docs, and adapters. Do
not add benchmark/evidence packets, private deployment references, paid-tier
internals, or generic memory positioning to the product tree.

Before submitting changes, run:

```bash
./scripts/ci-local.sh
```
