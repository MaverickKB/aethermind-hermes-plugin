# Security

Do not report secrets by opening public issues that include the secret material.
Use a private channel selected by the maintainer until a public security contact
is published.

AetherMind is local-first. Store files may contain sensitive project context if
users write sensitive content into them. Treat `.aethermind/` as project data and
review it before sharing a repository or artifact.
